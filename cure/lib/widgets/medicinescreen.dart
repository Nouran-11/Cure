import 'package:flutter/material.dart';

class Medicinescreen extends StatelessWidget {
  const Medicinescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is the medicine screen'));
  }
}
